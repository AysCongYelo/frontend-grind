"use strict";

// VARIABLES

// let = pwedeng baguhin ang value
let age = 25;
age = 26;

console.log(age); // 26

// const = hindi pwedeng i-reassign
const pi = 3.14;
// pi = 5; // Error

console.log(pi); // 3.14

// var = old way (avoid using)
var oldWay = "hello";

console.log(oldWay);

// ====================
// DATA TYPES
// ====================

// Number (integer)
let int = 25;

// Number (decimal/float)
let float = 2.5;

// Boolean (true or false)
let isStudent = true;
let isLoggedIn = false;

// Array (collection of values)
const fruits = ["Apple", "Mango", "Banana"];

// Object (key-value pairs)
const user = {
  name: "Ace",
  age: 23,
};

// null (intentional empty value)
let selectedUser = null;

// undefined (variable exists but has no value)
let score;

// ====================
// EXERCISE
// ====================

const firstName = "Eduard Ace";
const userAge = 23;
const isStudying = true;

console.log(firstName);
console.log(userAge);
console.log(isStudying);
